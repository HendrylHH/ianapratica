const menuButton=document.querySelector('.menu-button');const menu=document.querySelector('#menu');if(menuButton&&menu){menuButton.addEventListener('click',()=>{const open=menu.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{menu.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}))}
const categoryTitle=document.querySelector('#category-title');if(categoryTitle){const params=new URLSearchParams(location.search);const area=params.get('area')||'Tecnologia';const sub=params.get('sub')||'Codex';categoryTitle.textContent=area;document.querySelector('#subcategory-title').textContent=sub;document.title=`${area}: ${sub} — IA na Prática`}
const tocLinks=[...document.querySelectorAll('.toc a')];const articleSections=[...document.querySelectorAll('#article section[id]')];if(tocLinks.length&&articleSections.length){const setActive=id=>tocLinks.forEach(link=>link.classList.toggle('active',link.getAttribute('href')===`#${id}`));let clickedId='';let clickTimer;const observer=new IntersectionObserver(entries=>{if(clickedId)return;const visible=entries.filter(e=>e.isIntersecting).sort((a,b)=>a.boundingClientRect.top-b.boundingClientRect.top);if(visible[0])setActive(visible[0].target.id)},{rootMargin:'-30% 0px -60% 0px',threshold:0});articleSections.forEach(section=>observer.observe(section));tocLinks.forEach(link=>link.addEventListener('click',()=>{clickedId=link.hash.slice(1);setActive(clickedId);clearTimeout(clickTimer);clickTimer=setTimeout(()=>{clickedId=''},900)}))}
